import { SafeAreaView } from "react-native";
import { Text, TextInput, Button } from "react-native-paper";

export default function OrderQueuePage({ navigation }) {

    return (
        <SafeAreaView>
            <Text>Order Queue Page</Text>
        </SafeAreaView>
    );
}